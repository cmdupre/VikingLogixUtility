<UserControl x:Class="VikingLogixUtility.Views.UDT.UdtSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VikingLogixUtility.Views.UDT"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel
            Grid.Row="0"
            Grid.Column="0"
            Orientation="Horizontal">
            <Label
                VerticalContentAlignment="Center"
                Content="IP Address:"/>
            <TextBox
                VerticalContentAlignment="Center"
                Width="100px"
                TextChanged="Address_TextChanged"
                Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"/>
            <Button 
                Content="Read"
                Command="{Binding ReadClicked}"
                />
            <Button 
                Content="Write"
                Command="{Binding WriteClicked}"
                />
        </StackPanel>
    </Grid>
</UserControl>
